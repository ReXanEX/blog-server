# blog-server
A simple blog server built with FastAPI and PostgreSQL, fully containerized with Docker. It provides basic CRUD endpoints for managing posts, persistent storage via Docker volumes, and integrated logging. Includes a complete docker.
